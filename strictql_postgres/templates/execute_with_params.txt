from asyncpg import Connection


async def ${function_name}(connection: Connection, ${ ", ".join([f"{param.name_in_function}: {param.type_.__name__}" for param in params])}) -> str:
    return await connection.execute("${query}", ${", ".join([param.name_in_function for param in params])})
